# Dockerfile

# 1. Base Image: Gunakan image yang lebih lengkap (tidak 'slim')
# Ini membantu memastikan semua build tool dan apt tersedia
FROM python:3.12

# 2. Instal System Dependencies yang Paling Komprehensif
# Kami juga menambahkan dependency build essential dan dependensi FFMPEG/OpenCV 
# yang paling sering hilang, termasuk yang terkait X11/GUI untuk environment headless.
RUN apt-get update && \
    apt-get install -y \
        build-essential \
        libatlas-base-dev \
        libsm6 \
        libxext6 \
        libgl1-mesa-glx \
        ffmpeg \
        --no-install-recommends && \
    rm -rf /var/lib/apt/lists/*

# 3. Direktori Kerja
WORKDIR /app

# 4. Instal Python Dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 5. Salin Kode Aplikasi
COPY . .

# 6. Perintah Run (Port 8000)
EXPOSE 8000
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]